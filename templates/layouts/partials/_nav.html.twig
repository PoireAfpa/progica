  <nav class="navbar navbar-default navbar-trans navbar-expand-lg sticky-top">
    <div class="container">
      <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarDefault" aria-controls="navbarDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span></span>
        <span></span>
        <span></span>
      </button>
      <a class="navbar-brand text-brand color-a" href="{{ path('app_default') }}"><img src="https://i.ibb.co/qNKkNwZ/poire.png" alt="poire" border="0" height="50px"><span> PROGICA</span></a>
      <div class="navbar-collapse collapse justify-content-center" id="navbarDefault">
        <ul class="navbar-nav">

          <li class="nav-item">
            <a class="nav-link {% if current_menu is defined and current_menu == 'accueil' %}active{% endif %}" href="{{ path('app_default') }}">Accueil</a>
          </li>

         <!-- <li class="nav-item">
            <a class="nav-link" href="#">A propos</a>
          </li>-->

          <li class="nav-item">
            <a class="nav-link {% if current_menu is defined and current_menu == 'products' %}active{% endif %}" href="{{ path('app_product_index') }}">Gîtes</a>
          </li>

         <!-- <li class="nav-item">
            <a class="nav-link" href="{{ path('app_contact_index') }}">Proprietaire</a>
          </li>-->
          {% if is_granted('ROLE_USER')%}
           <li class="nav-item">
            <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
          </li>
          {% else %}

          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_login') }}">Login</a>
          </li>          
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_register') }}">Nouveau compte</a>
          </li>
          {% endif %}


        
    
      {% if is_granted('ROLE_ADMIN')%}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('admin') }}">Admin</a>
          </li>
      {% endif %}

      {% if is_granted('ROLE_OWNER')%}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('owner') }}">Espace Propriétaire</a>
          </li>
      {% endif %}
          </ul>
      </div>
      <button type="button" class="btn btn-b-n navbar-toggle-box navbar-toggle-box-collapse" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01">
        <i class="bi bi-search"></i>
      </button>

    </div>
  </nav>