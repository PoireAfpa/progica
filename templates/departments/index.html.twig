{% extends 'base.html.twig' %}

{% block title %}Departments index{% endblock %}

{% block body %}
    <h1>Departments index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Name</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for department in departments %}
            <tr>
                <td>{{ department.id }}</td>
                <td>{{ department.code }}</td>
                <td>{{ department.name }}</td>
                <td>{{ department.slug }}</td>
                <td>
                    <a href="{{ path('app_departments_show', {'code': department.code}) }}">show</a>
                    <a href="{{ path('app_departments_edit', {'code': department.code}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_departments_new') }}">Create new</a>
{% endblock %}
