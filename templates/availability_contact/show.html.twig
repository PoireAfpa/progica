{% extends 'base.html.twig' %}

{% block title %}AvailabilityContact{% endblock %}

{% block body %}
    <h1>AvailabilityContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ availability_contact.id }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ availability_contact.day }}</td>
            </tr>
            <tr>
                <th>HourStart</th>
                <td>{{ availability_contact.hourStart }}</td>
            </tr>
            <tr>
                <th>HourEnd</th>
                <td>{{ availability_contact.hourEnd }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_availability_contact_index') }}">back to list</a>

    <a href="{{ path('app_availability_contact_edit', {'id': availability_contact.id}) }}">edit</a>

    {{ include('availability_contact/_delete_form.html.twig') }}
{% endblock %}
